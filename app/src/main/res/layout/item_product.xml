<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="product"
            type="com.mis.route.e_commerce.domain.model.product.Product" />
    </data>

    <!--    -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="165dp"
        android:layout_height="170dp"
        android:layout_marginEnd="16dp"
        app:cardCornerRadius="15dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/product_cover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/gray"
                android:scaleType="centerCrop"
                app:imageFromUrl="@{product.imageCover}"
                tools:src="@drawable/image_home_appliance_1" />
            <TextView
                android:id="@+id/productName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{product.title}"
                android:textSize="22sp"
                app:layout_constraintTop_toBottomOf="@id/product_cover"
                app:layout_constraintStart_toStartOf="@+id/product_cover"
                app:layout_constraintEnd_toEndOf="@+id/product_cover"
                />
            <ImageView
                android:id="@+id/addToFavorites"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:visibility="invisible"
                android:src="@drawable/ic_favorites_selected"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>